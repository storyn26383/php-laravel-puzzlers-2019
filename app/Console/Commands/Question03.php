<?php

namespace App\Console\Commands;

use Illuminate\Console\Command;

class Question03 extends Command
{
    protected $signature = 'Question03';

    public function handle()
    {
        // $foo = [0];
        // $bar = [1];

        // echo count($foo + $bar) + count(array_merge($foo, $bar));

        // A) 2
        // B) 3
        // C) 4
        // D) None of the above
    }
}
